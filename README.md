# json-dsl-rule-engine-example
This example demonstrates how a JSON Domain-Specific Language (DSL) can define business rules. It includes a Python rule engine that interprets and executes these JSON-defined rules against input data. The example highlights that while rules are expressed in a 'no-code' JSON format, underlying 'code' is still required to process and apply them, ill
